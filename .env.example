# Google Cloud SQL Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control (it's in .gitignore)

# OPTION A: Cloud SQL Python Connector (Recommended - no proxy needed)
# Get this from: Cloud SQL Console > Instance > Connection name
INSTANCE_CONNECTION_NAME=your-project:your-region:your-instance
DB_USER=appuser
DB_PASS=your-database-password
DB_NAME=appdb

# OPTION B: Cloud SQL Auth Proxy (run proxy separately: cloud-sql-proxy INSTANCE_CONNECTION_NAME --port=3306)
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_USER=appuser
# DB_PASS=your-database-password
# DB_NAME=appdb

# OPTION C: Direct IP (add your IP to Cloud SQL "Authorized networks")
# DB_HOST=your-cloud-sql-public-ip
# DB_PORT=3306
# DB_USER=appuser
# DB_PASS=your-database-password
# DB_NAME=appdb

# Google Cloud authentication (required for Option A)
# Run: gcloud auth application-default login
# Or set path to service account key:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
